<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @actor.name %>
</p>

<p>
  <b>Dob:</b>
  <%= @actor.dob %>
</p>

<b>Roles</b>
<ul>
  <% @actor.roles.each do |role| %>
    <li>as <%= role.character_name %> in <%= role.movie.title %></li>
  <% end %>
</ul>

<%= link_to 'Edit', edit_actor_path(@actor) %> |
<%= link_to 'Back', actors_path %>
